package com.ncme.springboot.mapper;

import com.ncme.springboot.model.ExamQuestionPoint2;

public interface ExamQuestionPoint2Mapper {
    int insert(ExamQuestionPoint2 record);

    int insertSelective(ExamQuestionPoint2 record);
}